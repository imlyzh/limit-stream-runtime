[package]
name = "limit-stream-runtime"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "limit_stream_runtime"
path = "src/lib.rs"

[dependencies]
rmp = "0.8.12"
